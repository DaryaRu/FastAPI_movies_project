[pytest]
asyncio_mode = auto
testpaths = src

python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = -p no:aiohttp

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
